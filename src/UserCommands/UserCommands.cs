
namespace wowwowwow.UserCommands
{
    public class UserCommands
    {
        protected DataManager dataManager = new DataManager();
        static protected CommandManager commandManager = new CommandManager();
        protected VerboseManager verboseManager = new VerboseManager();
        protected VerboseManager.EmbedMessage embedMessage = new VerboseManager.EmbedMessage();
    }
}
